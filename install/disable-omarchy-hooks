#!/usr/bin/env bash
set -euo pipefail

hooks_root="${XDG_CONFIG_HOME:-$HOME/.config}/omarchy/hooks"

for path in \
  "$hooks_root/post-update.d/dhh-shell-commentary" \
  "$hooks_root/theme-set.d/dhh-shell-commentary" \
  "$hooks_root/font-set.d/dhh-shell-commentary" \
  "$hooks_root/battery-low.d/dhh-shell-commentary" \
  "$hooks_root/post-boot.d/dhh-shell-commentary"; do
  if [ -e "$path" ]; then
    rm -f "$path"
    printf 'removed %s\n' "$path"
  fi
done

